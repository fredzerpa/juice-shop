name: Juice Shop CI
description: CI/CD pipeline for OWASP Juice Shop

on: [push, pull_request]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - run: echo "Running tests..."
      - run: echo "Tests passed!"
